# 12 · 페이지 패턴 (memoir)

기획서 2.2 의 카테고리 10개 + 인증 화면. 모든 화면은 `[메인 헤더 60][서브 헤더 44]` 아래에서 시작하고, 첫 콘텐츠까지 32.

## 1. ☁️ 나의 공간 (메인 진입)
```
서브 헤더: 없음 (또는 오늘 날짜 label 우측)
컨테이너 full, 배경 cream (또는 랜딩 블롭 옅게)
  display-md "좋은 아침이에요, 승우" + caption "2026.09.19 금요일"   ← 아래 space.8
  벤토 그리드 (3열 lg / 2열 md / 1열)
    [wide] 오늘의 할일 — title + 체크리스트 5개 + "할 일 추가" ghost sm
    코드 스니펫 즐겨찾기 — 미리보기 카드 3 (제목 + 언어 배지)
    명령어 즐겨찾기 — 명령어 행 3 (mono + 복사)
    URL 북마크 즐겨찾기 — 슬림 카드 3
  각 벤토 우상단 "전체 보기 →" link
빈: 07 §2
```

## 2. 📅 캘린더
```
서브 헤더: [‹] 2026년 9월 [›]  · 오늘(ghost sm)  · 우측 뷰 전환(월/주 세그먼트)
컨테이너 full: [월간 그리드 1fr | 패널 320 (lg 이상 고정, 아래는 바텀시트)]
  셀 96: 날짜 label-md 좌상단(오늘 로즈 원), 이벤트 칩 1줄 최대 3 + "+N"
  패널: headline "19일 금요일" → 할일 체크리스트 → 메모 카드(단순 메모 규격) → "추가" secondary
```

## 3. 📝 일반 메모 (즉시 진입 에디터)
```
서브 헤더: 폴더 브레드크럼(일반 메모 › 프로젝트 › 회의록) · 우측 저장 상태 caption · ⋯
컨테이너 lg, 배경 zone, 본문 prose 680 (lg 이상 좌 10% 우 5%)
  headline 제목 인풋(underline 없음, placeholder "제목 없음")   ← 아래 space.6
  editor 본문 블록들 (블록 사이 8)
  선택 시 플로팅 툴바(글래스 필)
  하단 space.16 여유
리스트 화면 없음. 최근 문서 전환은 서브 헤더 브레드크럼 드롭다운.
```

## 4. ✍️ 단순 메모
```
서브 헤더: 정렬(최신/제목) 세그먼트 · 태그 칩 스크롤
컨테이너 md, zone 배경
  카드 세로 리스트: 패딩 24, 사이 24, body 6줄 clamp, 하단 태그 + 날짜 label
  FAB "새 메모" → 인라인 카드가 상단에 생겨 바로 타이핑(모달 없음)
```

## 5. 💻 코드 스니펫 (v4 시안)
```
서브 헤더: 언어 칩 [ALL(ink)] [JAVA] [PYTHON] … 가로 스크롤
컨테이너 xl, zone 배경
  히어로(첫 진입·상단): display-md "Your Digital / **Archival Ledger**." + body-lg secondary 2줄   ← 아래 space.12
  카드 리스트(사이 24): [언어 배지 + title] → 코드 블록(접힘 240) → body-sm 설명 → 태그 · OCT 24, 2023
  카드 hover 없음(클릭 대상은 제목·복사)
  헤더 New → WideModal(제목 underline, 언어 select, CodeMirror, 설명, 태그)
```

## 6. ⌨️ 명령어 메모
```
서브 헤더: 플랫폼 칩 [ALL] [MAC] [LINUX] [WINDOWS] [DOCKER] …
컨테이너 full, zone 배경
  그룹 라벨 label "GIT"   ← 아래 space.3
  명령어 행 52 × N, 행 사이 8
  그룹 사이 space.8
  복사 클릭 → check 1.5초. 행 클릭 → 인라인 편집(underline 인풋)
```

## 7. 🔗 URL 북마크
```
서브 헤더: 태그 칩 · 우측 뷰 전환(카드/리스트)
컨테이너 xl
  상단 URL 붙여넣기 인풋(filled, 전폭, placeholder "링크를 붙여넣으면 정보를 가져와요")   ← 아래 space.8
  슬림 카드 3열(xl)/2열(md)/1열, gap 16/24
  카드 클릭 → 새 탭. ⋯ → 편집·삭제
```

## 8. 🎬 영상 메모
```
서브 헤더: 태그 칩
컨테이너 xl
  썸네일 갤러리 3열/2열/1열, gap 24. 썸네일 16:9 radius 16 + 길이 배지
  아래 title-sm 2줄 + caption 채널 · 날짜
  클릭 → WideModal: 좌 플레이어 16:9, 우 메모(editor) + 타임스탬프 링크
```

## 9. 🔐 보안 메모
```
서브 헤더: 잠금 상태 배지(status.secure) "🔒 잠김" / "🔓 14:32 까지" 카운트다운 · 우측 "지금 잠그기" ghost sm
컨테이너 xl, zone
  잠김: 가로 행 72 × N (제목 보임, 본문 블러) + 중앙 오버레이 카드(글래스 480): shield 48 + title-lg "보안 구역이에요" + PIN 6칸 + primary lg "인증하기"
  해제: 행 블러 걷힘(600ms), 클릭 → 상세 모달. 세션 5분 무활동 시 자동 잠금
  2FA 미설정: 오버레이 카드가 "2FA 설정하기" 안내
```

## 10. 🙈 프라이빗
```
서브 헤더: 폴더 브레드크럼(프라이빗 › 2026 › 일기)
컨테이너 md
  폴더 타일 그리드 160, gap 16 (루트) → 안에서 단순 메모 카드 리스트 + 태그 필터
  "새 폴더" secondary sm 타일 마지막에
```

## 11. 로그인 · 회원가입 · 이메일 인증 · 2FA 설정
```
컨테이너 sm 480, 캔버스 cream + 블롭 옅게, 카드 없이(캔버스에 바로) 또는 글래스 카드
  로고 32   ← 아래 space.8
  headline "다시 만나서 반가워요"   ← 아래 space.2
  body-sm secondary 한 줄   ← 아래 space.8
  underline 인풋 × 2 (라벨 label 대문자), 사이 24
  primary lg 전폭 "로그인"   ← 위 space.8
  caption 링크 "비밀번호를 잊었나요?" · "가입하기"
회원가입: 이메일 → 인증 코드 PIN 6칸 → 비밀번호. 단계 표시 caption "2 / 3".
2FA 설정: QR(흰 카드 안 200×200) + 수동 키 code + PIN 확인 + 백업 코드 카드(다운로드 secondary).
오류: 폼 상단 danger 배너, 어느 쪽이 틀렸는지 노출 금지. PIN 은 shake.
```

## 12. 랜딩
```
헤더 60(로그인·시작하기 pill)
히어로: 블롭 2~3개(blob-pink·blob-blue, blur 80, 18s 부유) 위 display-lg 2줄(강조 단어 로즈) + body-lg + primary lg pill "시작하기" + 마스코트 200
섹션(사이 96): 카테고리 10개 소개 벤토 그리드(각 카테고리 색 원 + title + body-sm)
보안 섹션: shield + "이메일 인증 + 2FA" 사실 위주
푸터: 로고 + caption
```

## 13. 오류 페이지 (404 · 500 · 403 · 만료 링크)
```
canvas(cream + 블롭 옅게), 세로 가운데, 컨테이너 sm
  마스코트 160 (흰 원형 카드 안)   ← 아래 space.6
  display-sm 36 "여기엔 아무것도 없어요"   ← 아래 space.2
  body secondary 한 문장   ← 아래 space.8
  primary md "나의 공간으로" + ghost "이전 페이지"
  caption tertiary 오류 ID (500)   ← 위 space.8
```
| 상황 | 제목 | 설명 |
|---|---|---|
| 404 | "여기엔 아무것도 없어요" | "주소가 바뀌었거나 삭제된 메모예요" |
| 403 / 공유 만료 | "이 링크는 더 이상 유효하지 않아요" | "공유가 끝났거나 삭제됐어요" + "memoir 시작하기" |
| 500 | "문제가 생겼어요" | "잠시 후 다시 시도해 주세요 · ID abc123" + "다시 시도" |
| 오프라인(전체) | "오프라인이에요" | "연결되면 자동으로 이어져요". 에디터는 계속 쓸 수 있음 |

## 14. 마이페이지 · 활동
```
컨테이너 md
  헤더: 아바타 64 + headline 이름 + caption 이메일 · 가입일  우측 "프로필 편집" secondary sm
  밑줄 탭: 활동 · 통계 · 계정
  활동: 잔디(52주, 11 §4) + 아래 최근 활동 리스트(아이콘 원 32 + body-sm + caption 시각), 항목 사이 24
  통계: 카테고리별 메모 수 가로 막대 + 링(할일 완료율) 2열
```

## 15. 설정 · 보안 (`/settings/security`)
```
컨테이너 lg
  headline "설정"  (아래 space.6)
  좌 밑줄 탭 세로형(일반 · 보안 · 알림 · 데이터) 200 / 모바일 상단 세그먼트
  섹션(선 없이 32 여백): title + body-sm secondary
    설정 행 56: 좌 label-md + caption / 우 컨트롤
```
| 섹션 | 행 | 컨트롤 |
|---|---|---|
| 일반 | 테마 | 세그먼트 시스템/라이트/다크 |
| | 시작 카테고리 | 커스텀 Select |
| | 언어 | Select |
| 보안 | 비밀번호 변경 | 행 클릭 → Prompt 모달(현재·새·확인 underline) |
| | 2FA | Switch(켜면 설정 플로우 12 §11) + 백업 코드 "다시 받기" secondary sm |
| | 보안 구역 자동 잠금 | 세그먼트 1분/5분/15분 |
| | 로그인 기기 | 리스트(기기 + 시각 + "로그아웃" ghost sm) |
| 알림 | 이메일 알림 | Switch |
| 데이터 | 내보내기 | secondary sm "JSON 내보내기" |
| | 휴지통 | 링크 → 12 §16 |
| 위험 구역 | 탈퇴 | 마지막 섹션, `danger` outline 형 → 모달 + 비밀번호 + "탈퇴" 입력 |
즉시 반영은 Switch·세그먼트. 저장이 필요한 건 모달 안에서 끝낸다(페이지 하단 저장 버튼 없음).

## 16. 휴지통
컨테이너 md. 배너 neutral "30일이 지나면 자동으로 지워져요". 카드 리스트(카테고리 배지 + 제목 + 삭제일 `label`), 카드 hover 시 "복구" secondary sm · "영구 삭제" danger-ghost sm. 상단 "비우기" ghost sm → 확인 모달. 빈: "휴지통이 비어 있어요".

## 17. 태그 관리
컨테이너 md. 태그 칩 리스트(#태그 + 개수 caption), 클릭 → 인라인 이름 변경(underline), ⋯ → 병합·삭제. "태그 추가" secondary sm. 검색 filled 상단.

## 18. 메모 상세(모달) — 코드·북마크·영상·보안
WideModal 960. 좌 본문(코드 블록 전체 높이 / 영상 플레이어 / 북마크 미리보기 iframe 대신 OG 카드 + "열기") 우 속성 패널 280: 제목 underline, 언어/플랫폼 Select, 태그 입력 칩, 폴더, 즐겨찾기 Switch, 생성·수정 `label`. 하단 글래스 바: 좌 "삭제" danger-ghost, 우 "닫기" ghost(자동 저장). 모바일 풀스크린 + 상단 뒤로.

## 19. 공유 페이지 (`/share/[token]`)
06 §23. 상단 바 60 + 컨테이너 md + 카드 규격 + 액션 없음. 하단 "memoir 로 만들었어요" caption + 로고. 만료 시 13 표.

## 20. 검색 결과
헤더 검색 Enter → 컨테이너 xl. 카테고리 드롭다운이 특정 카테고리면 그 리스트가 필터링되고, "전체"면 아래 그룹 뷰. headline "‘debounce’ 검색 결과" + caption "12개". 필터 칩(전체·코드·북마크·…). 카테고리 그룹 `label` + 각 카테고리 카드 규격 3개 + "더 보기". 검색어 하이라이트 `blush` 배경. 없음: 07 §2.
